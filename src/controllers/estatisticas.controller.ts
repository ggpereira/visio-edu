import { Request, Response } from 'express';
import { getConnection, Connection } from 'typeorm';

// Retorna json com as estatísticas relacionadas a escolas
// async function getEstatisticasEscolas(req: Request, res: Response): Promise<Response> {
//     const conn: Connection = getConnection();



// }